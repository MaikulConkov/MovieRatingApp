<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MovieRatingApplication.Pages.RatedMovies"
             Title="RatedMovies"
             >
    <CollectionView ItemsSource="{Binding RatedMedias}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Grid Padding="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Image Grid.Row="0" Source="{Binding Media.Thumbnail}" Aspect="AspectFill" HeightRequest="200" />

                    <Label Grid.Row="1" Text="{Binding Media.DisplayTitle}" FontAttributes="Bold" />

                    <Label Grid.Row="2" Text="{Binding Rating, StringFormat='Rating: {0}'}" />
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>